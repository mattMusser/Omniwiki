<h1>Every Wiki</h1>
<hr>
<div class="row">
	<div class="col-md-8">
		<% @wikis.each do |wiki| %>
			<div class="media">
				<div class="media-body">
					<h4 class="media-heading">
						<p><%= link_to wiki.title, wiki_path(wiki.id) %></p>
					</h4>
				</div>
			</div>
		<% end %>
	</div>
</div>
